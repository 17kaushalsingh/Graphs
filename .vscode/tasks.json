{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run C++ (With commons.cpp)",
            "type": "shell",
            "command": "g++ -std=c++11 -o program ${fileDirname}/program ${file} ${workspaceFolder}/commons.cpp && ${fileDirname}/program",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compiles the current C++ file along with commons.cpp and runs the executable"
        }
    ]
}
